---
description: "Start or continue an in-flow Trellis debugging session — selects the correct debug approach and returns to verification"
---

# /trellis-debug

You are executing the Trellis `*debug` command.

Read this file now:
- `.opencode/trellis/odd-debug/SKILL.md` — the debug command guide

Then execute the `*debug` protocol exactly as documented, starting from the state check.
