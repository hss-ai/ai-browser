---
name: "odd-swarm"
version: "1.0.1"
description: "Trellis swarm command. Build all independent outcomes in the current phase simultaneously using odd-flow parallel agents."
---

# /trellis-swarm

You are executing the Trellis `*swarm` command.

Execute this flow:

1. Read `.opencode/trellis/docs/runtime/build-entry.md`.
2. Execute the build-entry checks and swarm initialisation exactly as written.
3. Then read `.opencode/trellis/docs/build/build-protocol.md`.
4. Execute the swarm build path for independent outcomes in the current phase.
